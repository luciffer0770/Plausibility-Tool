{
  "name": "Bosch Plausibility Check (Python + noVNC)",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "remoteEnv": {
    "DISPLAY": ":99",
    "BOSCH_PLAUS_GEOMETRY": "1280x720",
    "BOSCH_PLAUS_MINSIZE": "1024x600"
  },
  "postCreateCommand": "pip install --no-cache-dir -r requirements.txt",
  "postStartCommand": "nohup /usr/local/bin/start-bosch-plausibility-desktop.sh </dev/null >>/tmp/bosch-plausibility-desktop.log 2>&1 & sleep 3",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  },
  "forwardPorts": [6080],
  "portsAttributes": {
    "6080": {
      "label": "noVNC — open in browser for GUI",
      "visibility": "public",
      "onAutoForward": "notify"
    }
  }
}
